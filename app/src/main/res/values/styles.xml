<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="DatePicker" parent="ThemeOverlay.Material3.MaterialCalendar">
        <item name="colorPrimary">@color/white</item>
        <item name="textFillColor">@color/black</item>
        <item name="android:textColor">@color/black</item>
        <item name="backgroundTint">@color/white</item>
    </style>

    <style name="MyRatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">#B7FFD700</item>
        <item name="colorControlActivated">#ffd700</item>
    </style>

    <style name="TableCellStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:padding">8dp</item> <!-- Опционально: задайте отступы -->
        <item name="android:background">@drawable/cell_background</item> <!-- Опционально: задайте фон -->
        <item name="android:gravity">center</item> <!-- Опционально: задайте выравнивание содержимого -->
    </style>

</resources>